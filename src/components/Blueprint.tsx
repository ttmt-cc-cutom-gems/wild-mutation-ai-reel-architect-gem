import { motion } from 'motion/react';
import { Sparkles, ImageIcon, Video, Smartphone, ArrowRight } from 'lucide-react';

const steps = [
  {
    id: '01',
    title: 'The AI Mind',
    subtitle: 'Gemini Gem Tour',
    description: 'The Gemini Gem automatically analyzes real-time social media trends and crafts a highly optimized, 3-clip reel concept.',
    icon: Sparkles,
    color: 'from-[#00FF00]/20 to-transparent'
  },
  {
    id: '02',
    title: 'Generating Visuals',
    subtitle: 'Google Flow',
    description: 'Copy the ultra-detailed image prompts generated by the Gem and head to Google Flow to generate stunning base images.',
    icon: ImageIcon,
    color: 'from-blue-500/20 to-transparent'
  },
  {
    id: '03',
    title: 'Animation Secrets',
    subtitle: 'Meta\'s Vibes.ai',
    description: 'Grab the image-to-video animation prompts and transform static images into fluid video clips.',
    icon: Video,
    color: 'from-purple-500/20 to-transparent'
  },
  {
    id: '04',
    title: 'Mobile Strategy',
    subtitle: 'Massive Reach',
    description: 'Assemble the clips natively in the Facebook/Instagram app. Add music, type in text overlays, and publish.',
    icon: Smartphone,
    color: 'from-orange-500/20 to-transparent'
  }
];

export default function Blueprint() {
  return (
    <section className="py-32 border-t border-white/10 relative overflow-hidden bg-black">
      {/* Background accents */}
      <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-[500px] bg-[#00FF00]/5 blur-[150px] pointer-events-none rounded-full" />
      
      <div className="max-w-7xl mx-auto px-6 md:px-8 relative z-10">
        <div className="mb-20 text-center flex flex-col items-center">
          <div className="text-[10px] uppercase tracking-widest text-[#00FF00] mb-4 font-bold border border-[#00FF00]/30 px-4 py-1.5 rounded-full bg-[#00FF00]/5">The Blueprint</div>
          <h2 className="text-4xl md:text-5xl lg:text-7xl font-black italic tracking-tighter uppercase text-white mb-6">4-Step Pipeline</h2>
          <p className="max-w-2xl text-gray-400 font-light text-sm md:text-base">An autonomous workflow designed to take you from a viral concept to a published reel in under 15 minutes.</p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8">
          {steps.map((step, index) => {
            const Icon = step.icon;
            
            return (
              <motion.div
                key={step.id}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true, margin: "-50px" }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                className="group relative bg-[#0A0A0A] border border-white/5 hover:border-white/20 rounded-3xl p-8 overflow-hidden transition-all duration-500 hover:-translate-y-2 flex flex-col h-full"
              >
                {/* Background Gradient */}
                <div className={`absolute top-0 right-0 w-64 h-64 bg-gradient-to-bl ${step.color} opacity-0 group-hover:opacity-100 transition-opacity duration-700 blur-[50px] rounded-full pointer-events-none -translate-y-1/2 translate-x-1/2`} />
                
                <div className="flex justify-between items-start mb-12 relative z-10">
                  <div className="w-14 h-14 rounded-2xl bg-white/5 border border-white/10 flex items-center justify-center group-hover:scale-110 group-hover:bg-white/10 transition-all duration-500">
                    <Icon className="w-6 h-6 text-white group-hover:text-[#00FF00] transition-colors" />
                  </div>
                  <span className="text-5xl font-black italic text-white/5 group-hover:text-white/10 transition-colors pointer-events-none">
                    {step.id}
                  </span>
                </div>
                
                <div className="relative z-10 flex-grow flex flex-col justify-end">
                  <div className="text-[#00FF00] text-xs font-mono font-bold mb-3">{step.subtitle}</div>
                  <h3 className="text-xl md:text-2xl font-bold uppercase tracking-tighter text-white mb-4">{step.title}</h3>
                  <p className="text-gray-400 text-sm font-light leading-relaxed">
                    {step.description}
                  </p>
                </div>
                
                {/* Connecting arrow for desktop (except last item) */}
                {index < steps.length - 1 && (
                  <div className="hidden lg:block absolute top-1/2 -right-3 z-20 translate-x-full">
                    <ArrowRight className="w-5 h-5 text-white/20" />
                  </div>
                )}
              </motion.div>
            );
          })}
        </div>
        
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="mt-20 flex justify-center"
        >
          <div className="inline-flex items-center gap-4 bg-[#0A0A0A] px-6 py-4 rounded-full border border-white/10 shadow-2xl">
            <div className="w-2.5 h-2.5 rounded-full bg-[#00FF00] animate-pulse shadow-[0_0_10px_rgba(0,255,0,0.5)]"></div>
            <span className="text-xs font-mono font-medium text-gray-400">System Activity: <span className="text-[#00FF00]">"Elemental Shapeshifting" trend detected...</span></span>
          </div>
        </motion.div>
      </div>
    </section>
  );
}
